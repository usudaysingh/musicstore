/* If wanted to run when page is loaded. */
        $(function() {
            // Handler for .ready() called. 
            visibility("detail");
        });

        function visibility(reportType) {
            if({{ user.username }} == "rajputs") {
                $('#admin').show();
            }
            else {
                $('#admin').hide();
            }
        }
