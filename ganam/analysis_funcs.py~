from ganam.models import songs
class ChartData(object):    
    def get_data():
        data = {'serial_numbers': [], 'mass': []}

        valves = songs.objects.all()

        for unit in valves:
            data['serial_numbers'].append(unit.serial_number)
            data['mass'].append(unit.mass)

        return data
